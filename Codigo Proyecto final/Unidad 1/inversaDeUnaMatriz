import numpy as np 

A=np.array([[2,-1],
            [1,3]])

b=np.array([1,2])

#obtenemos la matriz inversa
Ainv=np.linalg.inv(A)

#multiplicamos la matriz inversa por la matriz b
x=np.dot(Ainv,b)

#imprimimos el resultado de sistema de ecuaciones
print(x)